name: Latest blog post workflow
on:
  schedule: # Run workflow automatically
    - cron: '0 * * * *' # Runs every hour, on the hour
  workflow_dispatch: # Run workflow manually (without waiting for the cron to be called), through the Github Actions Workflow page directly
jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "https://dev.to/feed/correiajpv"
          template: <p align="left">
                    <a href="$url" title="$title"><img src="$cover_image" alt="$title" width="250px" align="right" /></a>
                    <a href="$url" title="$title"><strong>$title</strong></a>
                    <div><strong>$published_at</strong> | <strong>$edited_at</strong></div>
                    <br/>
                    $description
                    </p><br/><br/>
